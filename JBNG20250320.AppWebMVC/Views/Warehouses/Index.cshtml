@model IEnumerable<JBNG20250320.AppWebMVC.Models.Warehouse>

@{
    ViewData["Title"] = "Index";
}

<h1>Bodegas</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Notes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WarehouseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-success" asp-route-id="@item.WarehouseId">Editar</a> |
                <a asp-action="Details" class="btn btn-secondary" asp-route-id="@item.WarehouseId">Detalles</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.WarehouseId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
